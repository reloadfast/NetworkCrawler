# NetworkCrawler backend dependencies
# Pin major versions here; exact pins are locked in requirements.lock (generated by pip-compile)

# Web framework
fastapi==0.111.*
uvicorn[standard]==0.29.*

# Database
sqlalchemy==2.0.*

# Background scheduling
apscheduler==3.10.*

# Testing
pytest==8.*
pytest-cov==5.*
pytest-asyncio==0.23.*
httpx==0.27.*          # async test client for FastAPI

# Linting / formatting
ruff==0.4.*

# Security audit
pip-audit==2.*
